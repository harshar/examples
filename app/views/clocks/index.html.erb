<h1>Listing clocks</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @clocks.each do |clock| %>
  <tr>
    <td><%= clock.name %></td>
    <td><%= link_to 'Show', clock %></td>
    <td><%= link_to 'Edit', edit_clock_path(clock) %></td>
    <td><%= link_to 'Destroy', clock, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Clock', new_clock_path %>
